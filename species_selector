<select name="<% $select_name %>">
  <option value="all">all</option>
% my $index_species = $config->{index_species};
% foreach my $spec (@{$index_species}) {
%   my $long_name = $spec;
%   $long_name = ucfirst($long_name);
%   $long_name =~ s/_/ /g;
  <option value="<% $spec %>"><% $long_name %></option>
%  }
</select>

<%args>
  $select_name => "species"
</%args>